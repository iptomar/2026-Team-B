Require branch names to follow a standard convention (e.g., feature/, bugfix/).

Detail local environment setup: npm run full.

State that pull requests must pass the GitHub Actions CI/CD pipeline checks before merging into the release branch for Azure Web Apps deployment.
